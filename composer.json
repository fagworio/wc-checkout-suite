{
	"name": "wc-checkoutsuite/wc-checkoutsuite",
	"description": "Advanced checkout field management for WooCommerce.",
	"type": "wordpress-plugin",
	"license": "proprietary",
	"keywords": ["woocommerce", "checkout", "custom fields"],
	"require": {
		"php": ">=8.2"
	},
	"require-dev": {
		"dealerdirect/phpcodesniffer-composer-installer": "^1.0",
		"php-stubs/woocommerce-stubs": "^9.0",
		"phpcompatibility/phpcompatibility-wp": "^2.1",
		"phpstan/phpstan": "^1.11",
		"phpunit/phpunit": "^9.6",
		"squizlabs/php_codesniffer": "^3.10",
		"szepeviktor/phpstan-wordpress": "^1.3",
		"wp-coding-standards/wpcs": "^3.1"
	},
	"autoload": {
		"psr-4": {
			"WCCheckoutSuite\\": "src/"
		}
	},
	"autoload-dev": {
		"psr-4": {
			"WCCheckoutSuite\\Tests\\": "tests/"
		}
	},
	"config": {
		"allow-plugins": {
			"dealerdirect/phpcodesniffer-composer-installer": true
		},
		"sort-packages": true
	},
	"scripts": {
		"lint": "phpcs",
		"lint:fix": "phpcbf",
		"analyse": "phpstan analyse --no-progress --memory-limit=1G",
		"test": "phpunit",
		"check": [
			"@lint",
			"@analyse",
			"@test"
		]
	},
	"scripts-descriptions": {
		"lint": "Check the WordPress coding standards over the plugin and its unit tests.",
		"analyse": "Run static analysis with the WordPress and WooCommerce stubs.",
		"test": "Run the unit suite; it needs neither WordPress nor WooCommerce.",
		"check": "Run every quality gate that must pass before a merge."
	}
}
